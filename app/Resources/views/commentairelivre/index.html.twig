{% extends 'baseF.html.twig' %}

{% block body %}
    <h1>Commentairelivres list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Commentaire</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commentaireLivre in commentaireLivres %}
            <tr>
                <td><a href="{{ path('commentairelivre_show', { 'id': commentaireLivre.id }) }}">{{ commentaireLivre.id }}</a></td>
                <td>{{ commentaireLivre.commentaire }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('commentairelivre_show', { 'id': commentaireLivre.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('commentairelivre_edit', { 'id': commentaireLivre.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commentairelivre_new') }}">Create a new commentaireLivre</a>
        </li>
    </ul>
{% endblock %}
