{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commentairelivre</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaireLivre.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaireLivre.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commentairelivre_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('commentairelivre_edit', { 'id': commentaireLivre.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
